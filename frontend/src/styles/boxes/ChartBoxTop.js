import styled from 'styled-components/macro'

const ChartBoxTop = styled.div`
    padding: 10px 0;
    border-radius: 30px 30px 0 0;
    text-align: left;
    background: var(--white-85);
    box-shadow:
        var(--lightshadow-input),
        var(--darkshadow-input);
`

export default ChartBoxTop