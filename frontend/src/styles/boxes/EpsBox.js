import styled from 'styled-components/macro'

const EpsBox = styled.div`
    display: inline-block;
    vertical-align: top;
    margin: 30px;
    padding: 10px 12px 12px 8px;
    width: 400px;
    max-width: calc(100% - 60px);
    border-radius: 30px;
    box-shadow:
        var(--lightshadow-flat),
        var(--darkshadow-flat);
`

export default EpsBox